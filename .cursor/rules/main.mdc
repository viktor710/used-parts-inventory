# Правила разработки для проекта "Система учета б/у запчастей"

## 🎯 Общие принципы

### Язык и стиль
- **Всегда отвечай на русском языке** при общении с пользователем
- Используй технический русский язык для комментариев и документации
- Следуй принципам чистого кода и SOLID

### Архитектура проекта
- Следуй принципам **Next.js 14 App Router**
- Используй **TypeScript** для строгой типизации
- Придерживайся **модульной архитектуры** с разделением ответственности
- Следуй **принципу единственной ответственности** для каждого модуля
- Используй **Dependency Injection** для зависимостей

### Интеграция с внешними API и библиотеками
- **Всегда изучай документацию** используемых библиотек через Context7 инструменты
- **Используй Context7** для доступа к актуальной документации всех библиотек
- **Проверяй совместимость версий** перед интеграцией новых библиотек
- **Документируй интеграции** с внешними сервисами в соответствующих файлах

## 🛠 Технологический стек

### Frontend
- **Next.js 14** с App Router
- **React 18** с функциональными компонентами и хуками
- **TypeScript** для типизации
- **Tailwind CSS** для стилизации
- **Lucide React** для иконок

### Backend & Database
- **Prisma ORM** для работы с базой данных
- **PostgreSQL** как основная БД
- **Supabase** для облачной БД
- **Cloudinary** для управления изображениями

### Утилиты
- **Zod** для валидации данных
- **Winston** для логирования
- **Sharp** для обработки изображений

## 📁 Структура файлов и папок

### Организация кода
```
app/                    # Next.js App Router
├── api/               # API роуты (REST endpoints)
├── cars/              # Страницы автомобилей
├── parts/             # Страницы запчастей
├── reports/           # Страницы отчетов
├── globals.css        # Глобальные стили
├── layout.tsx         # Корневой layout
└── page.tsx           # Главная страница

components/            # React компоненты
├── layout/           # Компоненты layout (Header, Sidebar)
├── ui/               # Переиспользуемые UI компоненты
└── debug/            # Компоненты для отладки

lib/                  # Библиотеки и утилиты
├── database.ts       # Конфигурация БД
├── cloudinary.ts     # Конфигурация Cloudinary
├── validation.ts     # Схемы валидации Zod
└── logger.ts         # Конфигурация логирования

types/                # TypeScript типы
└── index.ts          # Основные типы проекта

utils/                # Утилиты
├── cn.ts            # Утилита для CSS классов
├── debounce.ts      # Утилита debounce
└── format.ts        # Утилиты форматирования
```

## 🎨 Стилизация и UI

### Tailwind CSS
- Используй **Tailwind CSS** для всех стилей
- Придерживайся **мобильного подхода** (Mobile First)
- Используй **кастомные CSS переменные** для цветов
- Применяй **responsive breakpoints**: xs, sm, md, lg, xl, 2xl

### Цветовая схема
```css
/* Основные цвета */
--primary: #0A2472      /* Синий - основной акцент */
--secondary: #0B6623    /* Зеленый - вторичный акцент */
--text-primary: #333333 /* Темно-серый - основной текст */
--text-secondary: #666666 /* Средне-серый - вторичный текст */
--border: #DDDDDD       /* Светло-серый - разделители */
--background: #FFFFFF   /* Белый - основной фон */
--background-secondary: #F9F9F9 /* Светло-серый - вторичный фон */
```

### Компоненты UI
- Создавай **переиспользуемые компоненты** в `components/ui/`
- Используй **TypeScript интерфейсы** для пропсов
- Применяй **forwardRef** для компонентов с ref
- Добавляй **aria-атрибуты** для доступности

## 🗄️ Работа с базой данных

### Prisma Schema
- Следуй **конвенциям именования** Prisma
- Используй **каскадные удаления** где необходимо
- Добавляй **индексы** для оптимизации запросов
- Применяй **валидацию** на уровне схемы

### Модели данных
```prisma
// Основные модели
- Car (Автомобили)
- Part (Запчасти)
- Supplier (Поставщики)
- Customer (Клиенты)
- Sale (Продажи)

// Enums
- BodyType (Тип кузова)
- FuelType (Тип топлива)
- PartCategory (Категория запчасти)
- PartCondition (Состояние запчасти)
- PartStatus (Статус запчасти)
```

### API роуты
- Создавай **RESTful API** в `app/api/`
- Используй **HTTP методы** правильно (GET, POST, PUT, DELETE)
- Добавляй **валидацию** входящих данных с Zod
- Обрабатывай **ошибки** и возвращай соответствующие HTTP коды

## 🖼️ Работа с изображениями

### Cloudinary
- Используй **Cloudinary** для загрузки и оптимизации изображений
- Применяй **трансформации** для разных размеров
- Добавляй **placeholder изображения** для загрузки
- Оптимизируй **форматы** (WebP, AVIF)

### Компоненты изображений
- Создавай **оптимизированные компоненты** для изображений
- Используй **lazy loading** для производительности
- Добавляй **fallback изображения** при ошибках
- Применяй **responsive изображения** с разными размерами

## 🔍 Валидация и типизация

### Zod схемы
- Создавай **строгие схемы валидации** для всех форм
- Используй **TypeScript inference** из Zod схем
- Добавляй **кастомные сообщения об ошибках** на русском языке
- Применяй **трансформации** данных при необходимости

### TypeScript
- Используй **строгую типизацию** везде
- Создавай **интерфейсы** для всех структур данных
- Применяй **generic типы** для переиспользуемых компонентов
- Добавляй **JSDoc комментарии** для сложных функций
- **НЕ используй any тип** в TypeScript

## 🏷️ Использование @tags в комментариях для Cursor

### Основные принципы
- **Всегда документируй функции** с помощью JSDoc комментариев
- **Используй @tags** для структурирования документации
- **Пиши комментарии на русском языке** для лучшего понимания
- **Связывай функции с контекстом** через теги

### Обязательные теги

#### @description - Описание функции
```typescript
/**
 * @description Создает новый автомобиль в базе данных с валидацией данных
 * @param carData - Данные автомобиля для создания
 * @param prisma - Экземпляр Prisma клиента
 * @returns Promise<Car> - Созданный автомобиль
 * @throws {ValidationError} - При ошибке валидации данных
 * @throws {DatabaseError} - При ошибке базы данных
 */
export async function createCar(carData: CarData, prisma: PrismaClient): Promise<Car> {
  // Реализация функции
}
```

#### @param - Параметры функции
```typescript
/**
 * @param {string} id - Уникальный идентификатор автомобиля
 * @param {CarUpdateData} updateData - Данные для обновления
 * @param {PrismaClient} prisma - Экземпляр Prisma клиента
 */
export async function updateCar(
  id: string, 
  updateData: CarUpdateData, 
  prisma: PrismaClient
): Promise<Car> {
  // Реализация функции
}
```

#### @returns - Возвращаемое значение
```typescript
/**
 * @returns {Promise<Car[]>} Массив всех автомобилей в системе
 * @returns {Promise<null>} null если автомобили не найдены
 */
export async function getAllCars(): Promise<Car[] | null> {
  // Реализация функции
}
```

#### @throws - Возможные ошибки
```typescript
/**
 * @throws {ValidationError} Когда данные не прошли валидацию
 * @throws {NotFoundError} Когда автомобиль не найден
 * @throws {DatabaseError} При ошибке подключения к БД
 */
export async function deleteCar(id: string): Promise<void> {
  // Реализация функции
}
```

### Дополнительные теги

#### @example - Примеры использования
```typescript
/**
 * @example
 * ```typescript
 * const car = await createCar({
 *   brand: 'Toyota',
 *   model: 'Camry',
 *   year: 2020,
 *   vin: '1HGBH41JXMN109186'
 * }, prisma);
 * 
 * console.log(car.id); // "uuid-123"
 * ```
 */
export async function createCar(carData: CarData): Promise<Car> {
  // Реализация функции
}
```

#### @since - Версия добавления
```typescript
/**
 * @since 1.2.0 - Добавлена поддержка VIN кодов
 * @since 1.3.0 - Добавлена валидация года выпуска
 */
export function validateCarData(data: CarData): boolean {
  // Реализация функции
}
```

#### @deprecated - Устаревшие функции
```typescript
/**
 * @deprecated Используйте createCarWithValidation вместо этой функции
 * @param carData - Данные автомобиля
 * @returns Promise<Car>
 */
export async function createCarOld(carData: CarData): Promise<Car> {
  // Устаревшая реализация
}
```

#### @see - Ссылки на связанные функции
```typescript
/**
 * @see {@link updateCar} для обновления автомобиля
 * @see {@link deleteCar} для удаления автомобиля
 * @see {@link getCarById} для получения по ID
 */
export async function createCar(carData: CarData): Promise<Car> {
  // Реализация функции
}
```

### Теги для компонентов React

#### @component - React компоненты
```typescript
/**
 * @component CarCard
 * @description Карточка для отображения информации об автомобиле
 * @param {CarCardProps} props - Свойства компонента
 * @param {Car} props.car - Данные автомобиля
 * @param {boolean} props.isEditable - Разрешить редактирование
 * @param {Function} props.onEdit - Callback для редактирования
 * @returns {JSX.Element} React компонент карточки
 */
export function CarCard({ car, isEditable, onEdit }: CarCardProps): JSX.Element {
  // Реализация компонента
}
```

#### @hook - React хуки
```typescript
/**
 * @hook useCarData
 * @description Хук для работы с данными автомобилей
 * @param {string} carId - ID автомобиля
 * @returns {UseCarDataResult} Объект с данными и методами
 * @example
 * ```typescript
 * const { car, loading, error, updateCar } = useCarData('car-123');
 * ```
 */
export function useCarData(carId: string): UseCarDataResult {
  // Реализация хука
}
```

### Теги для API роутов

#### @api - API эндпоинты
```typescript
/**
 * @api POST /api/cars
 * @description Создает новый автомобиль
 * @body {CarCreateData} carData - Данные для создания автомобиля
 * @returns {ApiResponse<Car>} Ответ с созданным автомобилем
 * @throws {400} При ошибке валидации
 * @throws {401} При отсутствии аутентификации
 * @throws {403} При недостатке прав
 * @throws {500} При внутренней ошибке сервера
 */
export async function POST(request: Request): Promise<Response> {
  // Реализация API роута
}
```

### Теги для типов и интерфейсов

#### @interface - TypeScript интерфейсы
```typescript
/**
 * @interface CarData
 * @description Основные данные автомобиля
 * @property {string} brand - Марка автомобиля
 * @property {string} model - Модель автомобиля
 * @property {number} year - Год выпуска
 * @property {string} vin - VIN код (17 символов)
 * @property {string} [description] - Описание (опционально)
 */
interface CarData {
  brand: string;
  model: string;
  year: number;
  vin: string;
  description?: string;
}
```

#### @type - TypeScript типы
```typescript
/**
 * @type CarStatus
 * @description Статус автомобиля в системе
 * @value 'available' - Доступен для продажи
 * @value 'sold' - Продан
 * @value 'reserved' - Зарезервирован
 * @value 'maintenance' - На обслуживании
 */
type CarStatus = 'available' | 'sold' | 'reserved' | 'maintenance';
```

### Теги для бизнес-логики

#### @business - Бизнес-правила
```typescript
/**
 * @business Правило валидации VIN кода
 * @description VIN код должен содержать ровно 17 символов
 * @business Правило проверки года выпуска
 * @description Год выпуска не может быть в будущем
 * @business Правило уникальности VIN
 * @description VIN код должен быть уникальным в системе
 */
export function validateCarBusinessRules(carData: CarData): ValidationResult {
  // Реализация бизнес-правил
}
```

#### @security - Безопасность
```typescript
/**
 * @security Проверка прав доступа
 * @description Только пользователи с ролью MANAGER+ могут создавать автомобили
 * @security Валидация входных данных
 * @description Все входные данные проходят строгую валидацию
 * @security Логирование действий
 * @description Все операции с автомобилями логируются
 */
export async function createCarSecure(carData: CarData, user: User): Promise<Car> {
  // Реализация с проверками безопасности
}
```

### Лучшие практики

#### 1. Структурированность
```typescript
/**
 * @description Краткое описание функции
 * 
 * @param {Type} paramName - Описание параметра
 * @returns {ReturnType} Описание возвращаемого значения
 * @throws {ErrorType} Описание возможных ошибок
 * 
 * @example
 * ```typescript
 * // Пример использования
 * ```
 * 
 * @see {@link relatedFunction} Ссылка на связанную функцию
 * @since 1.0.0 Версия добавления
 */
```

#### 2. Консистентность
- Используй **одинаковые теги** для похожих функций
- Следуй **единому стилю** документации
- Применяй **стандартные названия** для тегов

#### 3. Актуальность
- **Обновляй документацию** при изменении функций
- **Удаляй устаревшие** комментарии
- **Проверяй точность** примеров

#### 4. Интеграция с Cursor
- **Используй теги** для лучшего понимания контекста
- **Связывай функции** через @see теги
- **Добавляй примеры** для сложных функций
- **Документируй ошибки** для отладки

## 🚀 Производительность

### Оптимизация
- Используй **React.memo** для компонентов
- Применяй **useMemo** и **useCallback** для оптимизации
- Добавляй **динамические импорты** для больших компонентов
- Используй **Next.js Image** для оптимизации изображений

### Логирование
- Используй **Winston** для структурированного логирования
- Добавляй **разные уровни** логирования (error, warn, info, debug)
- Создавай **контекстную информацию** в логах
- Настрой **ротацию логов** для продакшена

## 🧪 Тестирование и отладка

### Отладка
- Используй **DebugPanel** компонент для разработки
- Добавляй **console.log** с контекстом
- Применяй **React DevTools** для отладки компонентов
- Используй **Prisma Studio** для работы с БД

### Скрипты
- Создавай **тестовые скрипты** в `scripts/`
- Добавляй **валидацию данных** через скрипты
- Используй **backup/restore** скрипты для БД
- Создавай **мониторинг** производительности

## 📦 Деплой и CI/CD

### Vercel
- Настрой **автоматический деплой** на Vercel
- Используй **environment variables** для конфигурации
- Добавляй **preview deployments** для тестирования
- Настрой **custom domains** при необходимости

### Environment
- Используй **.env.local** для локальной разработки
- Создавай **env.example** с примерами переменных
- Добавляй **валидацию** environment переменных
- Используй **разные конфигурации** для dev/prod

## 🔒 Безопасность

### Валидация
- **Всегда валидируй** входящие данные
- Используй **Zod** для серверной валидации
- Добавляй **санитизацию** пользовательского ввода
- Применяй **rate limiting** для API

### Аутентификация (будущее)
- Планируй **интеграцию с Supabase Auth**
- Используй **JWT токены** для сессий
- Добавляй **middleware** для защиты роутов
- Применяй **RBAC** (Role-Based Access Control)

## 📚 Документация

### Код
- Добавляй **JSDoc комментарии** для функций
- Создай **README** для каждого компонента
- Документируй **API endpoints** с примерами
- Обновляй **CHANGELOG.md** при изменениях

### Пользовательская документация
- Создавай **руководства пользователя**
- Добавляй **скриншоты** для сложных операций
- Документируй **процессы** работы с системой
- Создавай **FAQ** для частых вопросов

## 🎯 Приоритеты разработки

### Высокий приоритет
1. **Стабильность** - исправление критических багов
2. **Производительность** - оптимизация медленных операций
3. **UX/UI** - улучшение пользовательского опыта
4. **Безопасность** - валидация и защита данных

### Средний приоритет
1. **Новые функции** - добавление полезных возможностей
2. **Рефакторинг** - улучшение архитектуры кода
3. **Тестирование** - покрытие тестами
4. **Документация** - обновление документации

### Низкий приоритет
1. **Косметические улучшения** - мелкие UI изменения
2. **Оптимизации** - не критичные улучшения производительности
3. **Интеграции** - дополнительные сервисы
4. **Аналитика** - метрики и мониторинг

## 🔄 Workflow

### Разработка
1. **Создавай feature branch** для новых функций
2. **Пиши код** следуя правилам
3. **Тестируй** локально перед коммитом
4. **Создавай PR** с описанием изменений
5. **Получай review** от команды
6. **Мержи** в main после одобрения

### Релизы
1. **Обновляй версию** в package.json
2. **Обновляй CHANGELOG.md**
3. **Создавай git tag**
4. **Деплой** на Vercel
5. **Тестируй** в продакшене
6. **Уведомляй** команду о релизе

## 🚨 Критические правила

### Обязательные
- **Никогда не коммить** .env файлы с секретами
- **Всегда валидируй** пользовательский ввод
- **Обрабатывай ошибки** во всех async операциях
- **Используй TypeScript** для всех новых файлов
- **Следуй принципам** доступности (a11y)

### Запрещенные
- **Не используй** any тип в TypeScript
- **Не создавай** компоненты без типизации
- **Не игнорируй** ошибки линтера
- **Не коммить** код без тестирования
- **Не используй** устаревшие API

## 📞 Поддержка

### При проблемах
1. **Проверь логи** в консоли браузера
2. **Изучи документацию** используемых библиотек
3. **Создай issue** с подробным описанием
4. **Используй отладочные инструменты**
5. **Обратись к команде** за помощью

### Полезные ресурсы
- **Next.js документация**: https://nextjs.org/docs
- **Prisma документация**: https://www.prisma.io/docs
- **Tailwind CSS документация**: https://tailwindcss.com/docs
- **TypeScript документация**: https://www.typescriptlang.org/docs

## 🔗 Интеграция с внешними API и библиотеками

### Обязательные требования
- **Всегда изучай документацию** используемых библиотек через Context7 инструменты
- **Используй Context7** для доступа к актуальной документации всех библиотек
- **Проверяй совместимость версий** перед интеграцией новых библиотек
- **Документируй интеграции** с внешними сервисами в соответствующих файлах

### Процесс интеграции
1. **Изучи документацию** библиотеки через Context7
2. **Проверь совместимость** с текущим стеком
3. **Создай план интеграции** с учетом архитектуры проекта
4. **Реализуй интеграцию** следуя лучшим практикам
5. **Документируй изменения** в ARCHITECTURE.md, CONTEXT.md, DEPENDENCIES.md, DATA_FLOW.md
6. **Протестируй интеграцию** локально и в staging

### Примеры интеграций
- **Cloudinary**: Для управления изображениями
- **Supabase**: Для альтернативной базы данных
- **Winston**: Для структурированного логирования
- **Zod**: Для валидации данных
- **NextAuth.js**: Для аутентификации

## 🏗️ Архитектурные принципы

### Разделение ответственности
- **Презентационный слой**: Только отображение данных и обработка пользовательского ввода
- **Бизнес-логика**: В папке `lib/` - вся логика приложения
- **Данные**: Prisma схемы и миграции в `prisma/`
- **API**: Next.js API роуты в `app/api/`

### Принцип единственной ответственности
Каждый модуль отвечает за одну конкретную область:
- `lib/auth.ts` - только аутентификация и авторизация
- `lib/cars.ts` - только работа с автомобилями
- `lib/parts.ts` - только работа с запчастями

### Dependency Injection
Зависимости инжектируются через параметры функций:
```typescript
// Хорошо
export async function createCar(data: CarData, prisma: PrismaClient) {
  return await prisma.car.create({ data });
}

// Плохо
export async function createCar(data: CarData) {
  return await prisma.car.create({ data }); // Прямая зависимость
}
```

## 🎨 Паттерны проектирования

### Repository Pattern
Используется для абстракции доступа к данным:
```typescript
// lib/cars.ts
export class CarRepository {
  constructor(private prisma: PrismaClient) {}
  
  async findAll() {
    return await this.prisma.car.findMany();
  }
  
  async findById(id: string) {
    return await this.prisma.car.findUnique({ where: { id } });
  }
}
```

### Service Layer Pattern
Бизнес-логика инкапсулирована в сервисах:
```typescript
// lib/parts.ts
export class PartService {
  constructor(private prisma: PrismaClient) {}
  
  async createPart(data: CreatePartData) {
    // Валидация
    // Бизнес-правила
    // Создание записи
  }
}
```

### Factory Pattern
Для создания сложных объектов:
```typescript
// lib/validation.ts
export const createCarSchema = z.object({
  brand: z.string().min(1),
  model: z.string().min(1),
  year: z.number().min(1900).max(new Date().getFullYear() + 1),
  // ...
});
```

## 📝 Конвенции именования

### Файлы и папки
- **Компоненты**: PascalCase (`CarCard.tsx`, `PartList.tsx`)
- **Утилиты**: camelCase (`format.ts`, `validation.ts`)
- **API роуты**: kebab-case (`[id]/route.ts`)
- **Типы**: PascalCase (`CarData`, `PartStatus`)

### Переменные и функции
- **Константы**: UPPER_SNAKE_CASE (`DATABASE_URL`, `MAX_FILE_SIZE`)
- **Переменные**: camelCase (`carData`, `partList`)
- **Функции**: camelCase (`createCar`, `getPartsByCategory`)
- **Классы**: PascalCase (`CarRepository`, `PartService`)

### База данных
- **Таблицы**: snake_case (`cars`, `parts`, `sales`)
- **Колонки**: snake_case (`created_at`, `updated_at`)
- **Индексы**: `idx_` + snake_case (`idx_cars_brand_model`)

## 🌐 RESTful API принципы

### Стандартные HTTP методы
- **GET** `/api/cars` - получение списка автомобилей
- **POST** `/api/cars` - создание автомобиля
- **GET** `/api/cars/[id]` - получение конкретного автомобиля
- **PUT** `/api/cars/[id]` - обновление автомобиля
- **DELETE** `/api/cars/[id]` - удаление автомобиля

### Стандартные ответы
```typescript
// Успешный ответ
{
  success: true,
  data: CarData,
  message?: string
}

// Ошибка
{
  success: false,
  error: string,
  details?: any
}
```

## ⚠️ Обработка ошибок

### Иерархия ошибок
```typescript
// lib/errors.ts
export class AppError extends Error {
  constructor(
    message: string,
    public statusCode: number = 500,
    public code?: string
  ) {
    super(message);
  }
}

export class ValidationError extends AppError {
  constructor(message: string) {
    super(message, 400, 'VALIDATION_ERROR');
  }
}

export class NotFoundError extends AppError {
  constructor(resource: string) {
    super(`${resource} не найден`, 404, 'NOT_FOUND');
  }
}
```

### Middleware для обработки ошибок
```typescript
// middleware.ts
export async function errorHandler(error: Error, req: NextRequest) {
  if (error instanceof AppError) {
    return NextResponse.json({
      success: false,
      error: error.message,
      code: error.code
    }, { status: error.statusCode });
  }
  
  // Логирование неизвестных ошибок
  logger.error('Неизвестная ошибка:', error);
  
  return NextResponse.json({
    success: false,
    error: 'Внутренняя ошибка сервера'
  }, { status: 500 });
}
```

## 🔐 Улучшенная безопасность

### Шифрование и хеширование
- **Пароли**: Хешируются с bcrypt (cost factor 12+)
- **JWT токены**: Подписываются с коротким временем жизни
- **HTTPS**: Обязателен для всех соединений
- **Переменные окружения**: Никогда не коммитятся в репозиторий

### Защита от атак
- **SQL инъекции**: Предотвращаются Prisma ORM
- **XSS атаки**: Блокируются санитизацией HTML
- **CSRF атаки**: Защита через токены
- **Rate limiting**: Ограничение частоты запросов

### Аудит и мониторинг
- **Логирование действий**: Все операции пользователей
- **Мониторинг подозрительной активности**: Необычные паттерны
- **Алерты безопасности**: При критических событиях
- **Резервное копирование**: Регулярные бэкапы данных

## 📊 Мониторинг и производительность

### Метрики производительности
- **Время ответа API**: Целевые значения < 200ms
- **Время загрузки страниц**: Целевые значения < 2s
- **Использование памяти**: Мониторинг утечек
- **Размер бандла**: Оптимизация для мобильных устройств

### Инструменты мониторинга
- **Winston**: Структурированное логирование
- **Prisma Studio**: Мониторинг базы данных
- **React DevTools**: Профилирование компонентов
- **Lighthouse**: Аудит производительности

### Оптимизации
- **Кэширование**: Redis для часто запрашиваемых данных
- **Lazy loading**: Динамические импорты компонентов
- **Image optimization**: Sharp для обработки изображений
- **Database indexing**: Оптимизация запросов

## 🧪 Тестирование и качество кода

### Типы тестов
- **Unit тесты**: Тестирование отдельных функций
- **Integration тесты**: Тестирование взаимодействия модулей
- **E2E тесты**: Тестирование полного пользовательского сценария
- **Performance тесты**: Тестирование производительности

### Инструменты качества
- **ESLint**: Проверка стиля кода
- **Prettier**: Форматирование кода
- **TypeScript**: Строгая типизация
- **Husky**: Pre-commit хуки

### Стандарты качества
- **Покрытие тестами**: Минимум 80%
- **Lint ошибки**: 0 критических ошибок
- **TypeScript ошибки**: 0 ошибок компиляции
- **Performance**: Соответствие Lighthouse 90+

## 📚 Документирование и поддержка

### Обновление документации
После каждой значимой разработки **ОБЯЗАТЕЛЬНО** обновляй:
- **ARCHITECTURE.md**: Изменения в архитектуре
- **CONTEXT.md**: Новые паттерны и принципы
- **DEPENDENCIES.md**: Новые зависимости
- **DATA_FLOW.md**: Изменения в потоках данных

### JSDoc стандарты
- **Обязательные теги**: @description, @param, @returns, @throws
- **Дополнительные теги**: @example, @since, @deprecated, @see
- **Специальные теги**: @component, @hook, @api, @interface
- **Бизнес-теги**: @business, @security

### Поддержка разработчиков
- **README файлы**: Для каждого модуля
- **Примеры использования**: В JSDoc комментариях
- **Troubleshooting**: Решение частых проблем
- **Migration guides**: При breaking changes

---

**Последнее обновление**: Сентябрь 2024
**Версия правил**: 2.0.0
**Ответственный**: Команда разработки

